[
    {
        "name": "order-service-agent",
        "description": "Diagnoses and remediates issues with the order-processing service. Owns order lifecycle state, pod health, and retry logic.",
        "parameters": {
            "type": "object",
            "properties": {
                "debugId": {
                    "type": "string",
                    "description": "an Id which is identifier for debugging"
                }
            },
            "required": [
                "debugId"
            ]
        }
    }
]