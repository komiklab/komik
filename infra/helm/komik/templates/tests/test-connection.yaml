apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "komik.fullname" . }}-test-connection"
  labels:
    {{- include "komik.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "komik.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
