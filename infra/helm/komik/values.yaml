# Default values for komik.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# This will set the replicaset count more information can be found here: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/
replicaCount: 1

# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  repository: nginx
  # This sets the pull policy for images.
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

# This is for the secrets for pulling an image from a private repository more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
imagePullSecrets: []
# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""

# This section builds out the service account more information can be found here: https://kubernetes.io/docs/concepts/security/service-accounts/
serviceAccount:
  # Specifies whether a service account should be created.
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account.
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template.
  name: ""

# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: {}
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  # This sets the service type more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: ClusterIP
  # This sets the ports more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#field-spec-ports
  port: 80

# This block is for setting up the ingress for more information can be found here: https://kubernetes.io/docs/concepts/services-networking/ingress/
ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
    # - secretName: chart-example-tls
    #   hosts:
    #     - chart-example.local

# -- Expose the service via gateway-api HTTPRoute
# Requires Gateway API resources and suitable controller installed within the cluster
# (see: https://gateway-api.sigs.k8s.io/guides/)
httpRoute:
  # HTTPRoute enabled.
  enabled: false
  # HTTPRoute annotations.
  annotations: {}
  # Which Gateways this Route is attached to.
  parentRefs:
    - name: gateway
      sectionName: http
      # namespace: default
  # Hostnames matching HTTP header.
  hostnames:
    - chart-example.local
  # List of rules and filters applied.
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /headers
  #   filters:
  #   - type: RequestHeaderModifier
  #     requestHeaderModifier:
  #       set:
  #       - name: My-Overwrite-Header
  #         value: this-is-the-only-value
  #       remove:
  #       - User-Agent
  # - matches:
  #   - path:
  #       type: PathPrefix
  #       value: /echo
  #     headers:
  #     - name: version
  #       value: v2

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

# This is to setup the liveness and readiness probes more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
livenessProbe:
  httpGet:
    path: /
    port: http
readinessProbe:
  httpGet:
    path: /
    port: http

# This section is for setting up autoscaling more information can be found here: https://kubernetes.io/docs/concepts/workloads/autoscaling/
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

# Additional volumes on the output Deployment definition.
volumes: []
  # - name: foo
  #   secret:
  #     secretName: mysecret
  #     optional: false

# Additional volumeMounts on the output Deployment definition.
volumeMounts: []
  # - name: foo
  #   mountPath: "/etc/foo"
  #   readOnly: true

nodeSelector: {}

tolerations: []

affinity: {}

##### Dependencies ############
redis:      
  enabled: true
  architecture: standalone
  auth:
    enabled: false
  image:
    registry: docker.io
    repository: bitnamilegacy/redis
    tag: 8.2.1-debian-12-r0
postgresql:
  enabled: true
  image:
    registry: docker.io
    repository: bitnamilegacy/postgresql
    tag: 17.6.0-debian-12-r4
  architecture: standalone
  auth:
    username: komik
    password: komik
    database: komik
  primary:
    resources:
      requests:
        cpu: 250m
        memory: 512Mi
      limits:
        memory: 512Mi
    extendedConfiguration: |
      shared_buffers = 64MB
      max_connections = 200
      work_mem = 2MB
      maintenance_work_mem = 32MB
      effective_cache_size = 128MB
    initdb:
      scripts:
        init_database.sql: |
          -- initialize postgresql
          CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
          CREATE DATABASE temporal OWNER komik;
          CREATE DATABASE temporal_visibility OWNER komik;
          
temporal:
  enabled: true
  server:
    replicaCount: 1
    additionalInitContainers:
      - name: wait-for-postgresql
        image: busybox:1.36
        command:
          - sh 
          - -c 
          - |
            until nc -z komik-postgresql.komik.svc.cluster.local 5432; do echo "waiting for postgresql..."; sleep 2; done; echo "postgresql is ready";
    config:
      logLevel: "info"
      namespaces:
        create: true
        namespace:
          - name: default
            retention: 3d
      persistence:
        datastores:
          default:
            sql:
              manageSchema: true
              createDatabase: false
              pluginName: postgres12_pgx
              driverName: postgres12_pgx
              databaseName: temporal
              connectAddr: "komik-postgresql.komik.svc.cluster.local:5432"
              connectProtocol: tcp
              user: komik
              password: komik
              tls:
                enabled: false
          visibility:
            sql:
              manageSchema: true
              createDatabase: false
              pluginName: postgres12_pgx
              driverName: postgres12_pgx
              databaseName: temporal_visibility
              connectAddr: "komik-postgresql.komik.svc.cluster.local:5432"
              connectProtocol: tcp
              user: komik
              password: komik
              tls:
                enabled: false

  schema:
    useHelmHooks: false

  frontend:
    replicaCount: 1
  history:
    replicaCount: 1
  matching:
    replicaCount: 1
  worker:
    replicaCount: 1
  web: 
    enabled: true
    replicaCount: 1
    service:
      type: ClusterIP
      port: 8088
    config:
      auth: 
        enabled: false



nats:
  enabled: true
  config:
    jetstream:
      enabled: true
      fileStore:
        enabled: true
        dir: /data/jetstream
        pvc:
          enabled: true
          size: 1Gi
    websocket:
      enabled: true
      port: 9222
      noTls: true
  extraResources:
    - apiVersion: batch/v1
      kind: Job
      metadata:
        name: nats-stream-init
        annotations:
          "helm.sh/hook": post-install,post-upgrade
          "helm.sh/hook-weight": "5"
          "helm.sh/hook-delete-policy": before-hook-creation
      spec:
        ttlSecondsAfterFinished: 120
        template:
          spec:
            restartPolicy: OnFailure
            initContainers:
              # Wait until NATS is ready before running the stream creation
              - name: wait-for-nats
                image: natsio/nats-box:latest
                command:
                  - sh
                  - -c
                  - |
                    until nats -s nats://komik-nats:4222 rtt 2>/dev/null; do
                      echo "Waiting for NATS..."; sleep 2;
                    done
            containers:
              - name: stream-init
                image: natsio/nats-box:latest
                command:
                  - sh
                  - -c
                  - |
                    # Create stream if it doesn't already exist
                    nats -s nats://komik-nats:4222 stream info komik-stream 2>/dev/null || \
                    nats -s nats://komik-nats:4222 stream add komik-stream \
                      --subjects "komik.>" \
                      --storage file \
                      --replicas 1 \
                      --max-age 24h \
                      --retention limits \
                      --discard old \
                      --dupe-window 2m \
                      --defaults
